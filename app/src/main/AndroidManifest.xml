<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.hs.emirim.flowerbeen.byeruss_sqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Byeruss_sqlite">
        <activity android:name=".MemberListActivity"
            android:parentActivityName=".RoomListActivity"/>
        <activity android:name=".RoomListActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".RoomCreateActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".RoomFindActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>